'use strict';

const graphql = require('graphql');
const chai = require('chai');

const pokemon = require('./gql');

const pokemonType = pokemon.type;
const expect = chai.expect;

describe('Pokemon', () => {
	// Tests
});
